# 前端 E2E 测试伪代码示例

本文档展示如何为前端工单编写验证脚本的思路，而非完整实现代码。

---

## 登录模块测试

### 功能点 1: 正常登录流程

```typescript
describe('登录', () => {
  test('手机号+密码 登录成功', () => {
    // 1. 打开登录页
    // 2. 输入正确手机号和密码
    // 3. 点击登录
    // 4. 验证: 跳转到首页, 显示用户信息, Token 已存储
  });

  test('短信验证码登录', () => {
    // 1. 切换到短信登录 tab
    // 2. 输入手机号
    // 3. 点击获取验证码
    // 4. 输入正确验证码 (mock 或从测试接口获取)
    // 5. 点击登录
    // 6. 验证: 登录成功
  });
});
```

### 功能点 2: 验证码校验

```typescript
describe('验证码校验', () => {
  test('图形验证码错误 → 提示错误', () => {
    // 输入错误验证码 → 验证错误提示出现
  });

  test('验证码过期 → 提示过期', () => {
    // 获取验证码后等待过期 → 输入验证码 → 验证提示
  });

  test('验证码倒计时中 → 禁止重复点击', () => {
    // 点击获取验证码 → 按钮变为倒计时状态 → 再次点击 → 验证无响应或提示
  });

  test('验证码输入错误 3 次 → 需滑动拼图验证', () => {
    // 连续输入错误验证码 3 次 → 出现滑动拼图验证 → 完成拼图后可继续
  });
});
```

### 功能点 3: 密码错误处理

```typescript
describe('密码错误处理', () => {
  test('密码错误 → 提示账号或密码错误', () => {
    // 输入正确手机号 + 错误密码 → 验证错误提示
  });

  test('密码错误 5 次 → 账号锁定 30 分钟', () => {
    // 连续 5 次输入错误密码 → 验证锁定提示 → 锁定期间禁止登录
  });
});
```

### 功能点 4: 账号状态

```typescript
describe('账号状态', () => {
  test('账号不存在 → 提示账号不存在', () => {
    // 输入不存在的手机号 → 验证提示
  });

  test('账号已注销 → 提示账号已注销', () => {
    // 使用已注销账号登录 → 验证提示
  });

  test('账号已封禁 → 提示账号已被封禁', () => {
    // 使用已封禁账号登录 → 验证提示包含封禁原因和解封时间
  });
});
```

### 功能点 5: 并发与会话

```typescript
describe('并发与会话', () => {
  test('同一账号 A/B 设备同时登录 → 后登录者踢出前者', () => {
    // 设备 A 登录 → 设备 B 登录同一账号 → 设备 A 被踢出到登录页
  });

  test('Token 过期 → 自动跳转登录页', () => {
    // 手动修改 localStorage 中的 Token 为过期值 → 访问需认证页面 → 跳转登录
  });

  test('登出后 → Token 清除', () => {
    // 登录 → 登出 → 验证 localStorage 中的 Token 被清除
  });
});
```

### 功能点 6: 安全防护

```typescript
describe('安全防护', () => {
  test('XSS 注入 → 被转义不执行', () => {
    // 在手机号输入框输入 <script>alert(1)</script> → 提交 → 页面不弹窗
  });

  test('SQL 注入 → 被拒绝', () => {
    // 输入 SQL 注入 payload → 验证被当作普通字符串处理，不报错
  });
});
```

---

## 用户管理模块测试

### 功能点 1: 权限控制

```typescript
describe('权限控制', () => {
  test('普通用户访问用户管理页 → 403 或重定向', () => {
    // 用普通账号登录 → 访问 /admin/users → 验证无权限
  });

  test('管理员访问用户管理页 → 正常访问', () => {
    // 用管理员账号登录 → 访问 /admin/users → 验证正常
  });

  test('普通用户点击新建用户按钮 → 按钮不可见或禁用', () => {
    // 登录普通账号 → 验证无新建按钮或按钮禁用
  });
});
```

### 功能点 2: 用户列表

```typescript
describe('用户列表', () => {
  test('分页加载 → 每页 20 条', () => {
    // 打开用户管理页 → 验证显示 20 条记录
  });

  test('下一页 → 显示后续 20 条', () => {
    // 点击下一页 → 验证显示第 21-40 条
  });

  test('切换每页条数 → 重新加载', () => {
    // 切换为每页 50 条 → 验证列表更新
  });

  test('总数据量不足一页 → 显示全部', () => {
    // 假设只有 15 条数据 → 每页 20 条 → 显示全部 15 条
  });
});
```

### 功能点 3: 搜索功能

```typescript
describe('搜索功能', () => {
  test('按用户名精确搜索 → 返回匹配结果', () => {
    // 输入存在的用户名 → 点击搜索 → 验证结果
  });

  test('按用户名模糊搜索 → 返回包含关键词的结果', () => {
    // 输入 "张" → 返回所有姓张的用户
  });

  test('搜索结果为空 → 显示空状态', () => {
    // 输入不存在的用户名 → 验证显示空状态
  });

  test('搜索特殊字符 → 无 SQL 注入风险', () => {
    // 输入 "; DROP TABLE users; --" → 验证无异常
  });
});
```

### 功能点 4: 创建用户

```typescript
describe('创建用户', () => {
  test('必填项缺失 → 对应字段显示错误', () => {
    // 不填手机号直接提交 → 验证手机号字段错误提示
    // 不填用户名直接提交 → 验证用户名字段错误提示
  });

  test('用户名已存在 → 创建失败', () => {
    // 输入已存在的用户名 → 提交 → 验证错误提示
  });

  test('手机号格式错误 → 提示格式不正确', () => {
    // 输入 "12345" 作为手机号 → 提交 → 验证格式错误
  });

  test('邮箱格式错误 → 提示格式不正确', () => {
    // 输入 "abc" 作为邮箱 → 提交 → 验证格式错误
  });

  test('密码不符合要求 (少于 8 位) → 提示密码太短', () => {
    // 输入 "123" 作为密码 → 提交 → 验证提示
  });

  test('所有字段正确 → 创建成功', () => {
    // 填写所有正确信息 → 提交 → 验证成功提示 → 用户列表出现新用户
  });
});
```

### 功能点 5: 编辑用户

```typescript
describe('编辑用户', () => {
  test('修改邮箱 → 更新成功', () => {
    // 点击编辑 → 修改邮箱 → 保存 → 验证成功 → 刷新后邮箱已变更
  });

  test('修改为已存在的邮箱 → 更新失败', () => {
    // 修改邮箱为另一个已存在用户的邮箱 → 保存 → 验证错误提示
  });

  test('无修改 → 保存按钮禁用', () => {
    // 打开编辑框 → 不做任何修改 → 验证保存按钮禁用
  });
});
```

### 功能点 6: 删除用户

```typescript
describe('删除用户', () => {
  test('点击删除 → 弹出确认框', () => {
    // 点击删除按钮 → 验证出现确认对话框
  });

  test('取消删除 → 用户未被删除', () => {
    // 点击删除 → 点击取消 → 验证用户仍存在
  });

  test('确认删除 → 用户被删除', () => {
    // 点击删除 → 点击确认 → 验证成功提示 → 用户从列表消失
  });

  test('删除自己 → 提示不能删除自己', () => {
    // 用管理员删除自己的账号 → 验证提示不能删除自己
  });

  test('批量删除 → 需确认', () => {
    // 选中多个用户 → 点击批量删除 → 验证确认对话框
  });

  test('全选 → 所有行被选中', () => {
    // 点击全选 → 验证所有行checkbox被选中
  });
});
```

---

## 商品模块测试

### 功能点 1: 商品列表

```typescript
describe('商品列表', () => {
  test('正常展示 → 显示商品卡片 (图片/名称/价格)', () => {
    // 打开商品列表页 → 验证商品卡片包含图片、名称、价格
  });

  test('分类筛选 → 只显示该分类商品', () => {
    // 点击分类标签 → 验证 URL 参数变化 → 验证列表只显示该分类
  });

  test('价格排序 (升序/降序)', () => {
    // 选择价格升序 → 验证价格递增
    // 选择价格降序 → 验证价格递减
  });

  test('销量排序 → 按销量递减', () => {
    // 选择销量排序 → 验证商品按销量从高到低排列
  });

  test('分页加载 → 滑动到底部自动加载', () => {
    // 滚动到页面底部 → 验证加载更多商品
  });
});
```

### 功能点 2: 商品详情

```typescript
describe('商品详情', () => {
  test('点击商品卡片 → 打开详情页', () => {
    // 点击商品卡片 → 验证 URL 变化 → 验证详情内容
  });

  test('选择 SKU 规格 → 价格更新', () => {
    // 选择颜色 "黑色" → 选择存储 "256GB" → 验证价格变化
  });

  test('选择无货 SKU → 购买按钮禁用', () => {
    // 选择显示 "缺货" 的 SKU → 验证购买按钮禁用
  });

  test('数量选择 (超过库存) → 提示库存不足', () => {
    // 输入数量大于库存 → 验证提示库存不足
  });
});
```

### 功能点 3: 购物车

```typescript
describe('购物车', () => {
  test('加入购物车 → 成功提示 + 角标数字+1', () => {
    // 点击加入购物车 → 验证成功提示 → 验证购物车角标数字变化
  });

  test('修改商品数量 → 小计价格更新', () => {
    // 修改数量为 3 → 验证单价 × 3 = 小计
  });

  test('删除单个商品 → 确认后删除', () => {
    // 点击删除 → 确认 → 验证商品从列表消失
  });

  test('删除购物车全部商品 → 显示空购物车', () => {
    // 清空购物车 → 验证空状态提示
  });

  test('全选 → 结算按钮可用', () => {
    // 全选所有商品 → 验证结算按钮从禁用变为可用
  });

  test('部分选中 → 结算按钮显示选中数量', () => {
    // 选中 2 件商品 → 验证结算按钮显示 "结算(2)"
  });
});
```

### 功能点 4: 收藏功能

```typescript
describe('收藏功能', () => {
  test('点击收藏 → 商品被收藏', () => {
    // 点击收藏按钮 → 验证变红/实心 → 我的收藏页出现该商品
  });

  test('再次点击 → 取消收藏', () => {
    // 已收藏状态下点击 → 变回空心 → 我的收藏页无该商品
  });
});
```

---

## 订单模块测试

### 功能点 1: 下单流程

```typescript
describe('下单流程', () => {
  test('购物车结算 → 进入确认订单页', () => {
    // 点击结算 → 验证跳转确认订单页
  });

  test('填写收货信息 → 必填项校验', () => {
    // 不填收货人直接提交 → 验证提示
    // 不填手机号直接提交 → 验证提示
    // 不填地址直接提交 → 验证提示
  });

  test('选择支付方式 → 微信/支付宝/银行卡', () => {
    // 选择微信支付 → 提交 → 验证跳转到微信支付页面
  });

  test('提交订单 → 跳转支付页 → 支付成功 → 订单创建', () => {
    // 提交订单 → 模拟支付成功回调 → 验证订单创建 → 验证跳转订单列表
  });

  test('购物车为空 → 结算按钮禁用', () => {
    // 清空购物车 → 验证结算按钮禁用
  });
});
```

### 功能点 2: 订单列表

```typescript
describe('订单列表', () => {
  test('查看全部订单', () => {
    // 打开订单列表 → 验证显示所有订单 (不同状态)
  });

  test('状态筛选 → 只显示该状态订单', () => {
    // 点击 "待付款" → 验证列表只显示待付款订单
    // 点击 "待发货" → 验证列表只显示待发货订单
  });

  test('搜索订单号 → 返回匹配订单', () => {
    // 输入订单号 → 验证找到对应订单
  });

  test('空状态 → 显示空状态提示', () => {
    // 无订单时 → 验证显示空状态提示
  });
});
```

### 功能点 3: 订单操作

```typescript
describe('订单操作', () => {
  test('待付款订单 → 可取消', () => {
    // 找到待付款订单 → 点击取消 → 选择原因 → 确认 → 验证订单状态变为已取消
  });

  test('已发货订单 → 不可取消', () => {
    // 找到已发货订单 → 验证无取消按钮
  });

  test('申请退款 → 需填写原因', () => {
    // 已付款订单 → 点击申请退款 → 不填原因直接提交 → 验证提示
    // 填写原因 → 提交 → 验证成功 → 状态变为退款中
  });

  test('退款进度查看', () => {
    // 点击查看退款进度 → 验证显示退款流程 (申请→审核→退款→完成)
  });
});
```

---

## 通用测试场景

### 功能点: 前端防护

```typescript
describe('前端防护', () => {
  test('XSS 注入 (输入框)', () => {
    // 输入 <script> 标签 → 提交 → 页面不执行脚本
  });

  test('XSS 注入 (URL 参数)', () => {
    // 访问 ?name=<script> → 验证内容被转义
  });

  test('CSRF 防护 → 关键操作需 Token', () => {
    // 构造恶意表单提交 → 验证被拒绝或需 CSRF Token
  });

  test('越权访问 → A 用户不能操作 B 用户数据', () => {
    // 登录用户 A → 尝试通过 URL 操作用户 B 的数据 → 验证被拒绝
  });
});
```

### 功能点: 响应式布局

```typescript
describe('响应式布局', () => {
  test('桌面端 (1920px) → 正常显示', () => {
    // 设置 viewport 1920x1080 → 验证布局正常
  });

  test('平板端 (768px) → 单列布局', () => {
    // 设置 viewport 768x1024 → 验证变为单列布局
  });

  test('手机端 (375px) → 移动端适配', () => {
    // 设置 viewport 375x667 → 验证移动端布局
  });
});
```

### 功能点: 性能

```typescript
describe('性能', () => {
  test('首屏加载时间 < 3 秒', () => {
    // 使用 Performance API → 验证 FCP < 3s
  });

  test('图片懒加载 → 不在视口不加载', () => {
    // 滚动前后对比 → 验证不在视口的图片未发送请求
  });

  test('大量数据渲染 → 不卡顿', () => {
    // 渲染 1000 条数据 → 验证滚动流畅
  });
});
```

---

## 测试脚本结构模板

```typescript
// {module}.spec.ts
import { test, expect } from '@playwright/test';

/**
 * {模块名}测试
 * 覆盖: {功能列表}
 */

test.describe('{模块名}', () => {

  // ===== 功能场景 A =====
  test('{场景 A.1} - {预期结果}', () => {
    // Arrange: 准备测试数据/状态
    // Act: 执行操作
    // Assert: 验证结果
  });

  test('{场景 A.2} - {预期结果}', () => {
    // ...
  });

  // ===== 功能场景 B =====
  test('{场景 B.1} - {预期结果}', () => {
    // ...
  });
});
```

---

## 后端接口测试伪代码示例

见 `backend-api-example.md`